services:
  - type: web
    name: syllable-battle-relay
    runtime: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm run relay
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 3010
